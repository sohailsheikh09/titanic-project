[project]
name = "titanic-project"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "scikit-learn",
    "joblib",
    "pandas",
    "numpy"
]

[tool.vercel]
entrypoint = "api.predict:handler"